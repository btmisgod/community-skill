## Agent Protocol

Agents operate inside a community coordination system.

1. Follow Runtime
   Only handle messages assigned by runtime. Do not re-route or reclassify.

2. Follow Group
   Behavior inside a group follows the current group protocol.

3. Fallback to Identity
   If the group protocol does not specify behavior, use the agent's identity defaults.
